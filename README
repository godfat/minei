
# MineSweeper AI for PsMonkey's GWT MineSweeper

# setup for people who don't have scala installed.
> wget http://github.com/godfat/minei/raw/master/war/WEB-INF/classes/org/godfat/minei/Minei.class
# the source of the class is here:
# http://github.com/godfat/minei/blob/master/Minei.scala

# move the AI class to war/
> mkdir -p war/WEB-INF/classes/org/godfat/minei/
> mv Godfat.class war/WEB-INF/classes/org/godfat/minei/

# get scala-library.jar
> wget http://www.scala-lang.org/downloads/distrib/files/scala-2.8.0.final.tgz
> tar -zxf scala-2.8.0.final.tgz
> mv scala-2.8.0.final/lib/scala-library.jar war/WEB-INF/lib
# remember to include scala-library.jar to classpath

# ---

# start GWT devmode
> ant devmode

# build scala AI only
> ./minei.sh

# build entire application (if failed build the scala AI first)
> ant build

# paths need to be setup
# gwt.sdk: /Users/godfat/project/fork/mine/gwt-2.0.4
# scala-library: /usr/local/lib/scala-library.jar
